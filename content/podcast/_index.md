+++
title = "Why Can't They Just? (podcast)"
#
# The episode list now lives on the front page, so this section page is not
# rendered — /podcast/ is served as a redirect to / via the `aliases` entry in
# content/_index.md. Episodes themselves still live at /podcast/<slug>/, so
# every existing episode link keeps working.
#
[build]
  render = 'never'
  list = 'never'
+++
